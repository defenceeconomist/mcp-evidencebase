{
  "name": "evidencebase-frontend",
  "version": "1.0.0",
  "private": true,
  "description": "Bootstrap frontend for Evidence Base",
  "scripts": {
    "build": "echo \"Static frontend served by nginx\"",
    "test:e2e": "playwright test",
    "test:e2e:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0"
  }
}
